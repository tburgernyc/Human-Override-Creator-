# ============================================
# Human Override Creator — Environment Config
# ============================================
# Copy this file to `.env` and fill in your values.
# On Railway: set these in the Variables dashboard (NOT in a file).

# Google Gemini API Key (REQUIRED)
# Get yours at: https://aistudio.google.com/apikey
GEMINI_API_KEY=

# Proxy Server Port (local dev only — Railway sets PORT automatically)
PROXY_PORT=3001

# ─── Set automatically by Railway (do not set these manually) ─────────────────
# PORT=              (Railway injects the public-facing port)
# RAILWAY_ENVIRONMENT=production
